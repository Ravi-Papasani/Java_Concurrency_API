package WorkingThreads;

public class ConcurrencyVsParallelism {
    public static void main(String[] args) {
      /*
      Parallelism:
      => Running many different tasks at the same time
      => needs multiple cores
      Concurrency:
      => Multiple tasks in progress at the same time
      => you can achieve Concurrency through scheduler
      => needs single / multiple cores (scheduler has more work of scheduling and un scheduling jobs(process/threads) in single core)
      * */
    }
}
